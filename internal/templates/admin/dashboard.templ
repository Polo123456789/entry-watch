package templates

import "github.com/Polo123456789/entry-watch/internal/templates/common"

templ Dashboard() {
  @common.Layout("Admin", EmptyHeadTags(), common.Navbar()) {
    <section>
      <h1>Admin</h1>
    </section>
  }
}

templ EmptyHeadTags() {
  <!-- No additional head tags -->
}
